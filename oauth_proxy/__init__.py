"""oauth_proxy — a local OpenAI-compatible API server backed by Claude (OAuth).

See DESIGN.md for architecture. Public surface:

    from oauth_proxy.app import build_app, main
"""

__version__ = "0.1.0"
